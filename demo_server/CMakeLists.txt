
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(demo_server)

ADD_COMPILE_OPTIONS(-g -Wall)

SET(SRC_FILES demo_server.c)

ADD_EXECUTABLE(${CMAKE_PROJECT_NAME} ${SRC_FILES})
TARGET_LINK_LIBRARIES(${CMAKE_PROJECT_NAME} pthread)
